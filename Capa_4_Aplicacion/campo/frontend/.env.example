# =================================================================
# PROYECTO: Sistema IoT Autónomo - Cuenca Chancay-Huaral (Yaku Qhawaq)
# ARCHIVO:  .env.example  (Capa 4 - Aplicación / Frontend React)
# Copiar a `.env` y ajustar. NUNCA subir `.env` al repo.
# =================================================================

# URL base del microservicio FastAPI (Capa 3). Debe incluir el esquema
# (https://). El frontend deriva automáticamente wss:// para los WebSockets
# (/ws/telemetria y /ws/alertas).
VITE_API_BASE_URL=https://internet-of-thingsfinal-project-production-80a2.up.railway.app
